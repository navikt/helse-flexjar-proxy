ktor:
  deployment:
    port: 8080
  application:
    modules:
      - no.nav.helse.ApplicationKt.main

security:
  app_client_id: "azure-app-client-id"
  app_client_secret: "azure-app-client-secret"
  app_scope: "azure-app-scope"
  config_token_endpoint: "azure-openid-config-token-endpoint"
