ktor:
  deployment:
    port: 8080
  application:
    modules:
      - no.nav.helse.ApplicationKt.main

security:
  client_id: "azure-app-client-id"
  client_secret: "azure-app-client-secret"
  scope: "azure-app-scope"
  config_token_endpoint: "azure-openid-config-token-endpoint"
  cors_allow_host: "localhost:9000"
